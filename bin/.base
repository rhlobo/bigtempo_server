#!/bin/bash

export PROJECT_NAME='bigtempo_server'
export PROJECT_REPO="rhlobo/bigtempo_server"

export CURRDIR="$(pwd)"
export PROJECT_LOCATION="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )"
export PYTHONPATH="${PROJECT_LOCATION}"